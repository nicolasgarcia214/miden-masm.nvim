; Miden Assembly locals.
;
; Taken verbatim from the grammar's upstream `queries/locals.scm`
; (https://github.com/0xMiden/tree-sitter-masm @ 3dfc7c1) -- `@local.scope` is
; already Neovim's capture name, so no porting was required.

(procedure) @local.scope

(entrypoint) @local.scope

(if) @local.scope

(while) @local.scope

(repeat) @local.scope
